import { RojoDirective } from './rojo.directive';

describe('RojoDirective', () => {
  it('should create an instance', () => {
    const directive = new RojoDirective();
    expect(directive).toBeTruthy();
  });
});
